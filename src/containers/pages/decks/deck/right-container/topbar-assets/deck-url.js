import React from 'react';
import PropTypes from 'prop-types';

const DeckUrl = () =>{
  return (
    <div>
      <p>deck url</p>
    </div>
  )
};

export default DeckUrl;

DeckUrl.propTypes = {
  currentDeck: PropTypes.object
};