import {error} from './messages';

// export const handleVotingClick = (e, activeUser, elementId) =>{
//   let vote = e.currentTarget.id;
//   if(activeUser) {
//     rateElement(elementId, activeUser.uid, vote);
//   }
//   else {
//     error("You have to be signed in in order to vote!")
//   }
// };